<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <title>Chỉnh sửa từ</title>
</head>

<body>
    <div class="wrapper">
        <div class="container-fluid">
            <h3 class="title-of-content text-center">Chỉnh sửa từ</h3>
            <div class="container">
                <form action="/edit/<%=item._id %>" method="POST" class="row">
                    <div class="col-md-4">
                        <input placeholder="Tiếng anh" type="text" name="enWord" value="<%= item.enWord%>"
                            class="form-control">
                    </div>
                    <div class="col-md-4">
                        <input placeholder="Tiếng việt" type="text" name="vnWord" value="<%= item.vnWord%>"
                            class="form-control">
                    </div>
                    <div class="col-md-4 form-check form-switch checkbox">
                        <% if (item.studied) { %>
                            <input checked name="studied" class="form-check-input" type="checkbox"
                                id="flexSwitchCheckChecked">
                            <% } %>
                                <% if (!item.studied) { %>
                                    <input name="studied" class="form-check-input" type="checkbox"
                                        id="flexSwitchCheckChecked">
                                    <% } %>
                                        <label class="form-check-label mt-1"><b>Đã học</b></label>
                    </div>
                    <div class="col-md-2"><button type="submit" class="btn btn-success mt-2">Chỉnh sửa</button></div>
                </form>
            </div>
        </div>
    </div>
    <!-- CDN bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf"
        crossorigin="anonymous"></script>
</body>

</html>